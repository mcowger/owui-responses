[project]
name = "responses"
version = "1.0.0"
description = "OpenAI Responses API Manifold"
requires-python = ">=3.11"
dependencies = [
    "openai~=2.29",
    "pydantic~=2.12",
]

[dependency-groups]
dev = [
    "black>=26.5.1",
]
