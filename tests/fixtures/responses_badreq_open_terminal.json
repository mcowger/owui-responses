{
  "include": [
    "web_search_call.action.sources"
  ],
  "input": [
    {
      "role": "user",
      "content": [
        {
          "type": "input_text",
          "text": "what tools do you have"
        }
      ]
    }
  ],
  "instructions": "You have access to a computer running Linux 6.18.33-Unraid (x86_64) on host \"b60c19ba78e5\" as user 'unknown' with /bin/bash. Python 3.12.13 is available.\n\nUse your tools to directly interact with the system — run commands, read and write files, and search the filesystem. Prefer verifying the current state before making changes. When running commands, check the output to confirm success. If a command produces no output, that typically means it succeeded.",
  "model": "gpt-5.5",
  "parallel_tool_calls": true,
  "prompt_cache_key": "cba2b084-0b1f-4a69-97c3-65774d9090eb",
  "reasoning": {
    "summary": "auto",
    "effort": "medium"
  },
  "store": true,
  "stream": true,
  "tools": [
    {
      "type": "function",
      "name": "list_files",
      "description": "Return a structured listing of files and directories at the given path.",
      "parameters": {
        "type": "object",
        "properties": {
          "directory": {
            "type": [
              "string",
              "null"
            ],
            "description": "Directory path to list."
          }
        },
        "required": [
          "directory"
        ],
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "function",
      "name": "read_file",
      "description": "Read a file and return its contents. Supports text files and images (PNG, JPEG, WebP, etc.). For text files you can optionally request a specific line range. Images are returned as binary so you can view and analyze them directly. Use display_file to show a file to the user.",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "Path to the file to read."
          },
          "start_line": {
            "anyOf": [
              {
                "description": "First line to return (1-indexed, inclusive). Defaults to the beginning of the file."
              },
              {
                "type": "null"
              }
            ]
          },
          "end_line": {
            "anyOf": [
              {
                "description": "Last line to return (1-indexed, inclusive). Defaults to the end of the file."
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "required": [
          "path",
          "start_line",
          "end_line"
        ],
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "function",
      "name": "display_file",
      "description": "Open a file in the user's file viewer so they can see it. Use this when the user wants to view or look at a file. This does not return file content to you — use read_file if you need to read the content yourself.",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "Absolute path to the file to display."
          }
        },
        "required": [
          "path"
        ],
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "function",
      "name": "write_file",
      "description": "Write text content to a file. Creates parent directories automatically. Overwrites if the file already exists.",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "Absolute or relative path to write to. Parent directories are created automatically."
          },
          "content": {
            "type": "string",
            "description": "Text content to write to the file."
          }
        },
        "required": [
          "path",
          "content"
        ],
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "function",
      "name": "replace_file_content",
      "description": "Find and replace exact strings in a file. Supports multiple replacements in one call with optional line range narrowing.",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "Path to the file to modify."
          },
          "replacements": {
            "type": "array",
            "description": "List of find-and-replace operations to apply sequentially.",
            "items": {
              "type": "object",
              "properties": {
                "target": {
                  "type": "string",
                  "description": "Exact string to find. Must match precisely, including whitespace."
                },
                "replacement": {
                  "type": "string",
                  "description": "Content to replace the target with."
                },
                "start_line": {
                  "anyOf": [
                    {
                      "type": "integer"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "description": "Narrow the search to lines at or after this (1-indexed)."
                },
                "end_line": {
                  "anyOf": [
                    {
                      "type": "integer"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "description": "Narrow the search to lines at or before this (1-indexed)."
                },
                "allow_multiple": {
                  "type": [
                    "boolean",
                    "null"
                  ],
                  "description": "If true, replaces all occurrences. If false, errors when multiple matches are found."
                }
              },
              "required": [
                "target",
                "replacement",
                "start_line",
                "end_line",
                "allow_multiple"
              ],
              "additionalProperties": false
            }
          }
        },
        "required": [
          "path",
          "replacements"
        ],
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "function",
      "name": "grep_search",
      "description": "Search for a text pattern across files in a directory. Returns structured matches with file paths, line numbers, and matching lines. Skips binary files.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Text or regex pattern to search for."
          },
          "path": {
            "type": [
              "string",
              "null"
            ],
            "description": "Directory or file to search in."
          },
          "regex": {
            "type": [
              "boolean",
              "null"
            ],
            "description": "Use regex. Set false for literal search."
          },
          "case_insensitive": {
            "type": [
              "boolean",
              "null"
            ],
            "description": "Perform case-insensitive matching."
          },
          "include": {
            "anyOf": [
              {
                "description": "Glob patterns to filter files (e.g. '*.py'). Files must match at least one pattern."
              },
              {
                "type": "null"
              }
            ]
          },
          "match_per_line": {
            "type": [
              "boolean",
              "null"
            ],
            "description": "If true, return each matching line with line numbers. If false, return only the names of matching files."
          },
          "max_results": {
            "type": [
              "integer",
              "null"
            ],
            "description": "Maximum number of matches to return."
          }
        },
        "required": [
          "query",
          "path",
          "regex",
          "case_insensitive",
          "include",
          "match_per_line",
          "max_results"
        ],
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "function",
      "name": "glob_search",
      "description": "Search for files and subdirectories by name within a specified directory using glob patterns. Results will include the relative path, type, size, and modification time.",
      "parameters": {
        "type": "object",
        "properties": {
          "pattern": {
            "type": "string",
            "description": "Glob pattern to search for (e.g. '*.py')."
          },
          "path": {
            "type": [
              "string",
              "null"
            ],
            "description": "Directory to search within."
          },
          "exclude": {
            "anyOf": [
              {
                "description": "Glob patterns to exclude from search results."
              },
              {
                "type": "null"
              }
            ]
          },
          "type": {
            "anyOf": [
              {
                "description": "Type filter: 'file', 'directory', or 'any'."
              },
              {
                "type": "null"
              }
            ]
          },
          "max_results": {
            "type": [
              "integer",
              "null"
            ],
            "description": "Maximum number of matches to return."
          }
        },
        "required": [
          "pattern",
          "path",
          "exclude",
          "type",
          "max_results"
        ],
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "function",
      "name": "list_processes",
      "description": "Returns a list of all tracked background processes, including running, done, and killed.",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": [],
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "function",
      "name": "run_command",
      "description": "Run a shell command in the background and return a command ID.\n\nThis system is running Linux 6.18.33-Unraid (x86_64) on b60c19ba78e5 as user 'unknown' with /bin/bash. Python 3.12.13 is available.",
      "parameters": {
        "type": "object",
        "properties": {
          "wait": {
            "anyOf": [
              {
                "description": "Seconds to wait for the command to finish before returning. If the command completes in time, output is included inline. Null to return immediately."
              },
              {
                "type": "null"
              }
            ]
          },
          "tail": {
            "anyOf": [
              {
                "description": "Return only the last N output entries. Useful to limit response size when only recent output matters."
              },
              {
                "type": "null"
              }
            ]
          },
          "command": {
            "type": "string",
            "description": "Shell command to execute. Supports chaining (&&, ||, ;), pipes (|), and redirections."
          },
          "cwd": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Working directory for the command. Defaults to the server's current directory if not set."
          },
          "env": {
            "anyOf": [
              {
                "type": "object",
                "properties": {},
                "required": [],
                "additionalProperties": false
              },
              {
                "type": "null"
              }
            ],
            "description": "Extra environment variables merged into the subprocess environment."
          }
        },
        "required": [
          "wait",
          "tail",
          "command",
          "cwd",
          "env"
        ],
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "function",
      "name": "get_process_status",
      "description": "Returns new output since the last poll, process status, and exit code. Output is drained on read to keep memory bounded.",
      "parameters": {
        "type": "object",
        "properties": {
          "process_id": {
            "type": "string",
            "description": ""
          },
          "wait": {
            "anyOf": [
              {
                "description": "Seconds to wait for the process to finish before returning. Returns early if the process exits. Null to return immediately."
              },
              {
                "type": "null"
              }
            ]
          },
          "offset": {
            "type": [
              "integer",
              "null"
            ],
            "description": "Number of output entries to skip. Use next_offset from the previous response to get only new output."
          },
          "tail": {
            "anyOf": [
              {
                "description": "Return only the last N output entries. Useful to limit response size when only recent output matters."
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "required": [
          "process_id",
          "wait",
          "offset",
          "tail"
        ],
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "function",
      "name": "send_process_input",
      "description": "Write text to the process's stdin. Include newline characters as needed.",
      "parameters": {
        "type": "object",
        "properties": {
          "process_id": {
            "type": "string",
            "description": ""
          },
          "input": {
            "type": "string",
            "description": "Text to send to the process's stdin. Include newline characters as needed."
          }
        },
        "required": [
          "process_id",
          "input"
        ],
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "function",
      "name": "kill_process",
      "description": "Terminate the process. Sends SIGTERM by default for graceful shutdown. Use force=true to send SIGKILL.",
      "parameters": {
        "type": "object",
        "properties": {
          "process_id": {
            "type": "string",
            "description": ""
          },
          "force": {
            "type": [
              "boolean",
              "null"
            ],
            "description": "Send SIGKILL instead of SIGTERM."
          }
        },
        "required": [
          "process_id",
          "force"
        ],
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "function",
      "name": "get_current_timestamp",
      "description": "Get the current Unix timestamp in seconds.",
      "parameters": {
        "properties": {},
        "type": "object",
        "required": [],
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "function",
      "name": "calculate_timestamp",
      "description": "Get the current Unix timestamp, optionally adjusted by days, weeks, months, or years.\nUse this to calculate timestamps for date filtering in search functions.\nExamples: \"last week\" = weeks_ago=1, \"3 days ago\" = days_ago=3, \"a year ago\" = years_ago=1",
      "parameters": {
        "properties": {
          "days_ago": {
            "default": 0,
            "description": "Number of days to subtract from current time (default: 0)",
            "type": [
              "integer",
              "null"
            ]
          },
          "weeks_ago": {
            "default": 0,
            "description": "Number of weeks to subtract from current time (default: 0)",
            "type": [
              "integer",
              "null"
            ]
          },
          "months_ago": {
            "default": 0,
            "description": "Number of months to subtract from current time (default: 0)",
            "type": [
              "integer",
              "null"
            ]
          },
          "years_ago": {
            "default": 0,
            "description": "Number of years to subtract from current time (default: 0)",
            "type": [
              "integer",
              "null"
            ]
          }
        },
        "type": "object",
        "required": [
          "days_ago",
          "weeks_ago",
          "months_ago",
          "years_ago"
        ],
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "function",
      "name": "list_knowledge_bases",
      "description": "List the user's accessible knowledge bases so a relevant internal source\ncan be chosen.",
      "parameters": {
        "properties": {
          "count": {
            "default": 10,
            "description": "Maximum number of KBs to return (default: 10)",
            "type": [
              "integer",
              "null"
            ]
          },
          "skip": {
            "default": 0,
            "description": "Number of results to skip for pagination (default: 0)",
            "type": [
              "integer",
              "null"
            ]
          }
        },
        "type": "object",
        "required": [
          "count",
          "skip"
        ],
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "function",
      "name": "search_knowledge_bases",
      "description": "Search the user's accessible knowledge bases by name and description to find\na relevant internal source.",
      "parameters": {
        "properties": {
          "query": {
            "description": "The search query to find matching knowledge bases",
            "type": "string"
          },
          "count": {
            "default": 5,
            "description": "Maximum number of results to return (default: 5)",
            "type": [
              "integer",
              "null"
            ]
          },
          "skip": {
            "default": 0,
            "description": "Number of results to skip for pagination (default: 0)",
            "type": [
              "integer",
              "null"
            ]
          }
        },
        "required": [
          "query",
          "count",
          "skip"
        ],
        "type": "object",
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "function",
      "name": "query_knowledge_bases",
      "description": "Search knowledge bases by semantic similarity to query.\nFinds KBs whose name/description match the meaning of your query.\nHelpful for discovering which knowledge base to query next.",
      "parameters": {
        "properties": {
          "query": {
            "description": "Natural language query describing what you're looking for",
            "type": "string"
          },
          "count": {
            "default": 5,
            "description": "Maximum results (default: 5)",
            "type": [
              "integer",
              "null"
            ]
          }
        },
        "required": [
          "query",
          "count"
        ],
        "type": "object",
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "function",
      "name": "grep_knowledge_files",
      "description": "Search for exact text across knowledge files. Returns matching lines with line numbers.\nUnlike query_knowledge_files (semantic/vector search), this performs exact string matching.\nAutomatically detects regex patterns (e.g. \"error|warn\", \"version \\d+\").\nHelpful for literal strings, identifiers, error messages, or regex-style searches.",
      "parameters": {
        "properties": {
          "pattern": {
            "description": "The text pattern to search for (regex auto-detected)",
            "type": "string"
          },
          "file_id": {
            "description": "Optional file ID to search within a single file only",
            "type": [
              "string",
              "null"
            ]
          },
          "case_insensitive": {
            "default": false,
            "description": "If true, ignore case when matching (default: false)",
            "type": [
              "boolean",
              "null"
            ]
          },
          "count_only": {
            "default": false,
            "description": "If true, return only match counts per file (default: false)",
            "type": [
              "boolean",
              "null"
            ]
          }
        },
        "required": [
          "pattern",
          "file_id",
          "case_insensitive",
          "count_only"
        ],
        "type": "object",
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "function",
      "name": "search_knowledge_files",
      "description": "Search files by filename across knowledge bases the user has access to.\nWhen the model has attached knowledge, searches only within attached KBs and files.\nHelpful when looking for a specific document or file name.",
      "parameters": {
        "properties": {
          "query": {
            "description": "The search query to find matching files by filename",
            "type": "string"
          },
          "knowledge_id": {
            "description": "Optional KB id to limit search to a specific knowledge base",
            "type": [
              "string",
              "null"
            ]
          },
          "count": {
            "default": 5,
            "description": "Maximum number of results to return (default: 5)",
            "type": [
              "integer",
              "null"
            ]
          },
          "skip": {
            "default": 0,
            "description": "Number of results to skip for pagination (default: 0)",
            "type": [
              "integer",
              "null"
            ]
          }
        },
        "required": [
          "query",
          "knowledge_id",
          "count",
          "skip"
        ],
        "type": "object",
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "function",
      "name": "query_knowledge_files",
      "description": "Search knowledge base files using semantic/vector search. Searches across collections (KBs),\nindividual files, and notes that the user has access to.\nHelpful for internal documentation, uploaded knowledge, and attached model knowledge.",
      "parameters": {
        "properties": {
          "query": {
            "description": "The search query to find semantically relevant content",
            "type": "string"
          },
          "knowledge_ids": {
            "description": "Optional list of KB ids to limit search to specific knowledge bases",
            "items": {
              "type": "string"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "count": {
            "default": 5,
            "description": "Maximum number of results to return (default: 5)",
            "type": [
              "integer",
              "null"
            ]
          }
        },
        "required": [
          "query",
          "knowledge_ids",
          "count"
        ],
        "type": "object",
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "function",
      "name": "view_knowledge_file",
      "description": "Get the content of a file from a knowledge base. Supports pagination for large files.",
      "parameters": {
        "properties": {
          "file_id": {
            "description": "The ID of the file to retrieve",
            "type": "string"
          },
          "offset": {
            "default": 0,
            "description": "Character offset to start reading from (default: 0)",
            "type": [
              "integer",
              "null"
            ]
          },
          "max_chars": {
            "default": 10000,
            "description": "Maximum characters to return (default: 10000, hard cap: 100000)",
            "type": [
              "integer",
              "null"
            ]
          },
          "line_numbers": {
            "default": false,
            "description": "If true, prefix each line with its 1-indexed line number",
            "type": [
              "boolean",
              "null"
            ]
          },
          "start_line": {
            "description": "Optional 1-indexed start line (overrides offset/max_chars when set)",
            "type": [
              "integer",
              "null"
            ]
          },
          "end_line": {
            "description": "Optional 1-indexed end line (inclusive)",
            "type": [
              "integer",
              "null"
            ]
          }
        },
        "required": [
          "file_id",
          "offset",
          "max_chars",
          "line_numbers",
          "start_line",
          "end_line"
        ],
        "type": "object",
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "function",
      "name": "search_chats",
      "description": "Search the user's previous chat conversations by title and message content.\nHelpful for finding details from earlier conversations.",
      "parameters": {
        "properties": {
          "query": {
            "description": "The search query to find matching chats",
            "type": "string"
          },
          "count": {
            "default": 5,
            "description": "Maximum number of results to return (default: 5)",
            "type": [
              "integer",
              "null"
            ]
          },
          "start_timestamp": {
            "description": "Only include chats updated after this Unix timestamp (seconds)",
            "type": [
              "integer",
              "null"
            ]
          },
          "end_timestamp": {
            "description": "Only include chats updated before this Unix timestamp (seconds)",
            "type": [
              "integer",
              "null"
            ]
          }
        },
        "required": [
          "query",
          "count",
          "start_timestamp",
          "end_timestamp"
        ],
        "type": "object",
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "function",
      "name": "view_chat",
      "description": "Get the full conversation history of a chat by its ID after a relevant\nprevious chat has been identified.",
      "parameters": {
        "properties": {
          "chat_id": {
            "description": "The ID of the chat to retrieve",
            "type": "string"
          }
        },
        "required": [
          "chat_id"
        ],
        "type": "object",
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "function",
      "name": "search_web",
      "description": "Search the public web for information. Best for current events, external references,\nor topics not covered in internal documents.",
      "parameters": {
        "properties": {
          "query": {
            "description": "The search query to look up",
            "type": "string"
          },
          "count": {
            "description": "Number of results to return (default: admin-configured value)",
            "type": [
              "integer",
              "null"
            ]
          }
        },
        "required": [
          "query",
          "count"
        ],
        "type": "object",
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "function",
      "name": "fetch_url",
      "description": "Fetch and extract the main text content from a web page URL.",
      "parameters": {
        "properties": {
          "url": {
            "description": "The URL to fetch content from",
            "type": "string"
          }
        },
        "required": [
          "url"
        ],
        "type": "object",
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "function",
      "name": "create_tasks",
      "description": "Create a visible task checklist for multi-step work so progress can be shown in chat.",
      "parameters": {
        "properties": {
          "tasks": {
            "description": "List of task items. Each item: content (string, required), status (pending|in_progress|completed|cancelled, default pending), id (optional, auto-generated).",
            "items": {
              "properties": {
                "id": {
                  "description": "Unique identifier for the task. Auto-generated if omitted.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "content": {
                  "description": "Task description.",
                  "type": "string"
                },
                "status": {
                  "default": "pending",
                  "description": "Task status.",
                  "enum": [
                    "pending",
                    "in_progress",
                    "completed",
                    "cancelled"
                  ],
                  "type": [
                    "string",
                    "null"
                  ]
                }
              },
              "required": [
                "id",
                "content",
                "status"
              ],
              "type": "object",
              "additionalProperties": false
            },
            "type": "array"
          }
        },
        "required": [
          "tasks"
        ],
        "type": "object",
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "function",
      "name": "update_task",
      "description": "Mark a single visible task item as completed, in_progress, pending, or cancelled.",
      "parameters": {
        "properties": {
          "id": {
            "description": "The task ID to update",
            "type": "string"
          },
          "status": {
            "default": "completed",
            "description": "New status: completed, in_progress, pending, or cancelled (default: completed)",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "id",
          "status"
        ],
        "type": "object",
        "additionalProperties": false
      },
      "strict": true
    },
    {
      "type": "web_search",
      "search_context_size": "medium",
      "user_location": {
        "type": "approximate",
        "country": "US",
        "city": "Lafayette",
        "region": "CA"
      }
    }
  ],
  "truncation": "auto",
  "user": "cba2b084-0b1f-4a69-97c3-65774d9090eb"
}

